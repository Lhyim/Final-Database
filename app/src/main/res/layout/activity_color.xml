<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/ButtonColor"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ColorActivity">

    <RadioGroup
        android:id="@+id/colorGroup"
        android:layout_width="318dp"
        android:layout_height="297dp"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">

        <TextView
            android:id="@+id/chooseView"
            android:layout_width="match_parent"
            android:layout_height="31dp"
            android:text="Choose your House!"
            android:textAlignment="center" />

        <RadioButton
            android:id="@+id/redrButton"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:text="Gryffindor" />

        <RadioButton
            android:id="@+id/bluerButton"
            android:layout_width="match_parent"
            android:layout_height="65dp"
            android:text="Ravenclaw" />

        <RadioButton
            android:id="@+id/greenrButton"
            android:layout_width="match_parent"
            android:layout_height="63dp"
            android:text="Slytherin" />

        <RadioButton
            android:id="@+id/yellowrButton"
            android:layout_width="match_parent"
            android:layout_height="59dp"
            android:text="Hufflepuff" />
    </RadioGroup>
</androidx.constraintlayout.widget.ConstraintLayout>